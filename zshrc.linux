# Linux-specific configuration

# asdf (standard install location)
. "$HOME/.asdf/asdf.sh"

# direnv
eval "$(direnv hook zsh)"

# SSH agent
eval $(ssh-agent -s) > /dev/null
ssh-add ~/.ssh/id_ed25519 2>/dev/null
